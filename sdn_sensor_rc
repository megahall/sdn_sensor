# options for GNU autotools builds
export CFLAGS="-g"
export CCFLAGS="-g"
export CPPFLAGS="-g"
export CXXFLAGS="-g"

script_directory="$(dirname $(readlink -f ${BASH_SOURCE[0]}))"
build_directory="${script_directory}"

export LOG4C_RCPATH="${script_directory}/conf/log4crc"
